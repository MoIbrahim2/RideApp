import { RequestRideInterceptor } from './request-ride.interceptor';

describe('RequestRideInterceptor', () => {
  it('should be defined', () => {
    expect(new RequestRideInterceptor()).toBeDefined();
  });
});
