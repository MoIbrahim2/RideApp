import { CaptainCancelRideInterceptor } from './captain-cancel-ride.interceptor';

describe('CaptainCancelRideInterceptor', () => {
  it('should be defined', () => {
    expect(new CaptainCancelRideInterceptor()).toBeDefined();
  });
});
