import { DocumentObjectOnRequestGuard } from './document-object-on-request.guard';

describe('DocumentObjectOnRequestGuard', () => {
  it('should be defined', () => {
    expect(new DocumentObjectOnRequestGuard()).toBeDefined();
  });
});
